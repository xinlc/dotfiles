# Rime schema
# encoding: utf-8


patch:
  
  # 空格键上会显示方案名称
  schema/name: Rime ice

  # 智能纠错
  translator/enable_correction: true

  key_binder:
    import_preset: default
    bindings/+:
      # 不要单引号了（相应主题为下滑 L 键），改成删除自造词
      - { when: composing, accept: "'", send: Shift+Delete }
